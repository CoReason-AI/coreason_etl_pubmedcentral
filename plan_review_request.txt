Ah! The `dummy` destination has these config defaults: `fail_schema_update: bool = False, fail_prob: float = 0.0, retry_prob: float = 0.0, completed_prob: float = 0.0, exception_prob: float = 0.0, timeout: float = 10.0, fail_terminally_in_init: bool = False, fail_transiently_in_init: bool = False`.
Wait! If `fail_prob: float = 0.0` and `exception_prob: float = 0.0`, why did it say `failed due to timeout`?
Ah, if `completed_prob = 0.0`, it never completes!!
It keeps returning `pending` until it hits `timeout` (10s), then it fails with `timeout`!
So I need `completed_prob=1.0`!

Let me configure dlt to use dummy with `completed_prob=1.0`.
In `tests/integration/test_live_pipeline.py`:
```python
    os.environ["DESTINATION__NAME"] = "dummy"
    os.environ["PMC_PIPELINE__DESTINATION"] = "dummy"
    os.environ["DESTINATION__DUMMY__COMPLETED_PROB"] = "1.0"
```
Or I can mock `dlt.pipeline` to use `destination=dlt.destinations.dummy(completed_prob=1.0)`.

Let's change `mock_pipeline` in the test!
